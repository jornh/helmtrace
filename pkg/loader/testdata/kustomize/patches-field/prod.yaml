apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  replicas: 5
  template:
    spec:
      containers:
        - name: myapp
          env:
            - name: LOG_LEVEL
              value: warn
            - name: FEATURE_FLAGS
              value: "new-ui,dark-mode"
